import {StyleSheet} from 'react-native';
import {SIZES} from '@ui';

export default StyleSheet.create({
  container: {
    paddingHorizontal: SIZES.MEDIUM,
    paddingTop: SIZES.BIGGER,
    paddingBottom: SIZES.SMALL,
  },
});
